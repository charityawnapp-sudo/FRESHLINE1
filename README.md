# Freshline Flutter App

This project has been converted from the original React/Vite prototype into a Flutter application.

## Run

```bash
flutter pub get
flutter run
```

## Verify

```bash
flutter analyze
flutter test
flutter build web
```

## Structure

- `lib/` Flutter application source
- `assets/images/` migrated images from the original design
